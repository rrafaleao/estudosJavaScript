// Tipos de dados;

const nome = 'Rafael' // STRING
const num = 12 // Numero
let undefined; // Indefinido -> não aponta para lugar nenhum na memoria 
let nulo = null; // Nulo -> não aponta para nenhum lugar na memoria 
let boleano = true; // Boleano